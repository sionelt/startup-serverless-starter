{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "start": {
      "dependsOn": ["$IS_LOCAL"],
      "outputs": [".build/**", ".sst/**"]
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**"]
    },
    "packages/aws-account-infra#bootstrap": {
      "dependsOn": ["$GITHUB_REPOSITORY"],
      "outputs": []
    }
  }
}
