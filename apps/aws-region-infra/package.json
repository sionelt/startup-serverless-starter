{
  "name": "aws-region-infra",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "bootstrap": "sst deploy --stage region"
  },
  "devDependencies": {
    "sst": "snapshot",
    "aws-cdk-lib": "2.50.0"
  }
}
